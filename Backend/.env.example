# ===========================================
# Variables pour Railway (Backend)
# ===========================================
# À recopier dans Railway → Service → Variables
# Ne jamais commiter le fichier .env (il est dans .gitignore)

# --- OBLIGATOIRES (sinon le backend ne démarre pas) ---
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/lecocondelaura
JWT_SECRET=votre-secret-long-et-aleatoire

# --- CORS (recommandé en prod) ---
# URL du front (Vercel). Ex: https://lecocondelaura.vercel.app
FRONTEND_URL=https://ton-front.vercel.app

# --- EMAILS (obligatoire si résa / notifications) ---
EMAIL_SERVICE=gmail
EMAIL_USER=ton-email@gmail.com
EMAIL_PASSWORD=mot-de-passe-ou-app-password
RECIPIENT_EMAIL=email-de-laura@example.com

# --- Site (liens dans les emails) ---
SITE_URL=https://lecocondelaura.fr

# --- Optionnel (RIB dans les emails) ---
# RIB="IBAN: FR76...\nBIC: ...\nTitulaire: ..."

# PORT est injecté automatiquement par Railway, ne pas le définir
